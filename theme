import flet as ft


def main(page: ft.Page):

    page.title = "Test"

    page.add(
        ft.Text(
            "FLET IS WORKING!",
            size=40,
        )
    )


if __name__ == "__main__":
    ft.run(
        main,
        view=ft.AppView.WEB_BROWSER,
    )